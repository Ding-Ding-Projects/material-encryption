# Roadmap

## Current 0.1.9 candidate

- [x] Preserve and materialize the complete design export.
- [x] Add a sandboxed Electron shell and an allowlisted VeraCrypt bridge.
- [x] Keep VeraCrypt passwords out of IPC and process arguments.
- [x] Add the original logo, nine-size Windows icon source, four chrome treatments, bounded local custom-image handling, fit/background controls, persistence, generated PNG sizes, and reset.
- [x] Expand the hand-written design inventory to 14 destinations, including Ollama Studio.
- [x] Replace the flat converter format list with eight searchable categories that distinguish bundled and unavailable adapters.
- [x] Add PDF inspect, split, merge, extract, reorder, rotate, and metadata tools with reviewed plans, atomic output, and reopened-output validation.
- [x] Add a resumable persistent conversion queue with incremental file/folder intake, bounded worker concurrency, storage preflight, cancellation, retry, crash recovery, and per-file outcomes.
- [x] Add Ollama Studio runtime discovery, exhaustive official catalog/tag pagination, offline cache, evidence-based PC-fit results, free batch download cart, local chat, reviewed harness profiles, configuration snapshots, automatic failed-launch rollback, and explicit restore.
- [x] Expand the Node test inventory to 55 passing tests across VeraCrypt validation, TOTP, logo, converter core/bridge, PDF tools and queue, and Ollama management.
- [x] Run `npm test` (55/55) and the complete `npm run test:all` suite successfully.
- [x] Capture the packaged renderer in 36 states across all 14 destinations: 24 packaged UI interactions, one actual bridge/runtime observation (healthy local Ollama `v0.32.9`), and 11 labelled seeded visual fixtures.
- [ ] Build and verify the unsigned Squirrel.Windows installer from the final commit.
- [ ] Publish and verify the next unique release, release assets, documentation site, and final commit links.

## Later

- Read mounted-volume metadata from a stable VeraCrypt-supported interface when one is available without parsing translated UI output.
- Add automatic update feed support after an immutable release establishes the feed contract.
- Expand persistent local history and export coverage while preserving the secret boundary.
- Add a capability-safe attachment picker to Ollama chat only after both the renderer and main-process bridge can validate model capability and local file selection end to end.
