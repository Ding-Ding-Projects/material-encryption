# Contributing

Open an issue before a large change. Keep patches focused, preserve the design-source workflow, run `npm run test:all`, and use `build.bat /s` for a release-equivalent local build. Never add secrets, real volume paths, bundled VeraCrypt binaries, remote fonts, tracking, code signing, or a password-bearing command argument.
